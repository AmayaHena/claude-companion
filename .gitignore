# local build output
/claude-companion

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# graft wiring for Claude Code (local to this machine, not shared)
/.claude/
/.mcp.json
/.ignore

# written by demo/play.sh while replaying the demo
/demo/projects/-demo/demo1111-*.jsonl
